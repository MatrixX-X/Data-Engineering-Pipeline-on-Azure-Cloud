SELECT
     *
FROM
    OPENROWSET(
        BULK 'https://oliststorageaccountdbda.dfs.core.windows.net/olistdata/silver/',
        FORMAT = 'PARQUET'
    ) AS result1

# create view

create schema gold;

create view gold.final 
as 
SELECT
     *
FROM
    OPENROWSET(
        BULK 'https://oliststorageaccountdbda.dfs.core.windows.net/olistdata/silver/',
        FORMAT = 'PARQUET'
    ) AS result1;

select * from gold.final;

# Create Credentials for Ext Table

-- CREATE MASTER KEY ENCRYPTION BY PASSWORD = 'd8TySvmWr4rPBUk';

-- CREATE DATABASE SCOPED CREDENTIAL OlistStorageCredential
-- WITH IDENTITY = 'Managed Identity';

select * from sys.database_credentials

# SQL to Gold Layer

CREATE EXTERNAL FILE FORMAT extfileformat WITH (
    FORMAT_TYPE = PARQUET,
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.SnappyCodec'
);

CREATE EXTERNAL DATA SOURCE goldlayer WITH (
    LOCATION = 'https://oliststorageaccountdbda.dfs.core.windows.net/olistdata/gold/',
    CREDENTIAL = OlistStorageCredential
);


CREATE EXTERNAL TABLE gold.finaltable WITH (
        LOCATION = 'Serving',
        DATA_SOURCE = goldlayer,
        FILE_FORMAT = extfileformat
) AS
SELECT * FROM gold.final;

select * from gold.finaltable;